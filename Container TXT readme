To run the container, you first need to pull the Github repo to your local file explorer and then change your current directory to the Github one.

You then need to run "docker build liams_container"
Then you can do "docker run liams_container"
